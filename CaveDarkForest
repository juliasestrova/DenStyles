:root {
  --color-1: #0a1819;
  --color-2: #1e332e;
  --color-3: #435441;
  --color-4: #778168;
  --color-5: #c2c2a8;
  --color-6: #feeae3;
  --color-7: white;
}

body {
  text-wrap: stable;
  background: url(https://cdn.pixabay.com/photo/2021/03/30/12/47/clouds-6136881_1280.jpg)
    fixed;
  background-repeat: no-repeat;
  background-size: 100%;
}

/*topbar (money, wiki and others)*/
.topbar {
  background: var(--color-2);
  color: var(--color-5);
  padding: 8px 15px;
  font-size: 14px;
  font-weight: 600;
  border-radius: 10px;
  border: 2px solid var(--color-2);
  margin-top: 0.2%;
}

/*user name/link*/
.welcomeback a font {
  color: var(--color-7);
}

/*messages,money, modbox, wiki, logout*/
.toplinks li a {
  color: var(--color-3) !important;
}

/*Currency*/
img.icon {
  filter: drop-shadow(2px 4px 6px var(--color-5));
}

/*Main container*/
/*Header main container*/
.main {
  /* background: #FCF8E3; */
  background: none;
  margin-top: 60px;
  border: 0px solid beige;
  box-shadow: none;
}

.row {
  margin-right: -15px;
  margin-left: -15px;
  background: none;
}

.navbar {
  margin-bottom: 0;
  border: 1px solid var(--color-1);
  border-radius: 5px;
  background: var(--color-2);
  text-transform: uppercase;
}

/*Logo*/
a.navbar-brand.visible-lg.visible-md img {
  filter: brightness(70%) saturate(60%) sepia(30%) hue-rotate(68deg)
    drop-shadow(2px 4px 6px var(--color-4));
}

.navbar-default .navbar-nav > li > a {
  color: var(--color-5);
}

/*linkbar*/
.nav li a font {
  color: var(--color-6);
}
.nav li a:hover {
  background: none !important;
  filter: drop-shadow(2px 4px 6px var(--color-5));
}

/*cave main info*/
.col-md-9 {
  background: none;
  padding-top: 25px;
}

.breadcrumb {
  background: var(--color-2);
  font-size: 13px;
  border-radius: 5px;
  border: 1px solid var(--color-1);
  color: var(--color-4);
}

.breadcrumb a {
  color: var(--color-5);
}

/*bookmark button*/
input.floatright {
  background: var(--color-3);
  color: var(--color-5);
  text-shadow: none;
}

.floatleft a img {
  filter: brightness(105%) saturate(70%) sepia(30%) hue-rotate(17deg);
}

.floatright a img {
  filter: brightness(105%) saturate(70%) sepia(30%) hue-rotate(17deg);
}

.center font {
  color: var(--color-6);
}

/*lion image*/
.featured-lion {
  background-color: var(--color-1);
  color: var(--color-5);
  padding: 0;
  margin: 0 auto;
  max-width: 640px;
  font-weight: normal;
  font-size: 15px;
}

.lion-image-block img:nth-child(1) {
  content: url(https://static.lioden.com/images/dynamic/backgrounds/BrumousColdMorning.png);
}

.lionImage {
  width: 100%;
  overflow: auto;
  text-align: center;
  margin-bottom: 10px;
  position: relative;
  z-index: 0;
  border: 5px solid var(--color-1);
  border-radius: 15px;
}

/*lions list*/
.table {
  border: 5px solid var(--color-1);
  background: var(--color-3);
  color: var(--color-5);
}

table tr th {
  background: var(--color-1) !important;
  color: var(--color-4) !important;
}

table tr th a {
  color: var(--color-4) !important;
}

.table .left {
  background: var(--color-2);
  border-radius: 0;
}

td.right_odd a font {
  color: var(--color-6);
}

td.right a font {
  color: var(--color-6);
}

.table .right_odd {
  background: none !important;
}

.table .bottom {
  text-align: center;
  background: var(--color-2);
  color: #fff;
}

.table button.button {
  background: var(--color-4);
  color: var(--color-1);
  text-shadow: none;
}

td.right_odd a:hover {
  filter: drop-shadow(2px 4px 6px var(--color-6));
}

td.right a:hover {
  filter: drop-shadow(2px 4px 6px var(--color-6));
}

/*cave statistic*/
.table .top,
.table th {
  background-color: var(--color-1);
  color: var(--color-7);
  padding: 8px 15px;
  font-size: 15px;
  font-weight: bold;
}

/*cave setting*/
input[type="text"] {
  color: var(--color-2);
  font-size: 14px;
  font-weight: 600;
}

textarea {
  color: var(--color-2);
  font-size: 16px;
}

/*sidebar*/
.sidebar {
  padding-top: 25px;
  background: var(--color-2);
}

.col-md-3 .panel {
  border: 1px solid var(--color-2);
  background: var(--color-3);
  box-shadow: none;
  color: var(--color-6);
}

a:link,
a:visited,
a:active {
  color: var(--color-1);
  text-decoration: none;
  font-weight: bold;
}

.panel a:hover {
  filter: drop-shadow(2px 4px 6px var(--color-4));
}

.panel a font {
  color: var(--color-2);
}

.sidebar h3 {
  color: var(--color-4);
  font-size: 20px;
  font-weight: 600;
  text-decoration: none;
  text-shadow: none;
}

.sidebar a {
  color: var(--color-1);
}

.sidebar a:hover {
  filter: drop-shadow(2px 4px 6px var(--color-1));
}

/*progres*/
.progress {
  background: var(--color-4);
}

.progress-bar {
  background: var(--color-2);
}

.progress div {
  width: 100%;
  position: absolute;
  text-align: center;
  color: var(--color-6);
  margin: 0;
  padding: 0;
  font-size: 11px;
}

/*chat*/

input#btnToggleSidebarChat {
  background: var(--color-3);
  color: var(--color-5);
  text-shadow: none;
}

/*not use main style root*/
.s-chat-sidebar #label_currentChannel {
  background: #9b6c7c;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-weight: bold;
  min-width: 140px;
  padding: 4px 12px;
  color: #e6b6b2;
}

#chatMessageContainer {
  border-radius: 6px;
  border: 1px solid #9b6c7c;
  overflow: hidden;
  position: relative;
}

input#btnChatSend {
  color: #9b6c7c;
  text-shadow: none;
  background: #e6b6b2;
  border: #9b6c7c;
}

a.btn.btn-default.button {
  color: #9b6c7c;
  text-shadow: none;
  background: #e6b6b2;
  border: #9b6c7c;
}

div#chatMessagePinned span {
  color: #9b6c7c;
  background: #e6b6b2;
  border: none;
}

#chatMessagePinned.s-chat-message__pinned {
  background: #9b6c7c url(//static.lioden.com/images/layout/sticky.png)
    no-repeat 10px 6px;
  border-bottom-color: #9b6c7c !important;
  color: #9b6c7c;
  font-weight: bold;
}
div#chatMessageContainer a {
  color: #c6726f;
}
.s-chat-message {
  border-bottom: 1px solid #9b6c7c;
  border-top: 1px solid #9b6c7c;
  border-left: 2px solid #9b6c7c;
  border-right: 2px solid #9b6c7c;
  padding: 4px 6px;
  position: relative;
  word-wrap: break-word;
  overflow-wrap: break-word;
  background: #e6b6b2 !important;
  color: #6a5870;
}
.s-chat-message_claim {
  background: #47455d;
  border-bottom: 1px solid #c6726f;
  color: #e6b6b2;
}

/*blocks*/
.container-block-main {
  position: relative;
  width: 100%;
  height: 800px;
}

.block-main {
  display: block;
  overflow: auto;
  padding: 8px;
  box-sizing: border-box;
  background-color: var(--color-2);
  color: var(--color-6);
  border: 5px solid var(--color-1);
  border-radius: 10px;
  text-align: center;
  scrollbar-color: var(--color-4) var(--color-2);
  line-height: 1;
}

.block1-info {
  position: absolute;
  width: 40%;
  height: 96%;
}

.block2-info {
  position: absolute;
  width: 57%;
  height: 48%;
  right: 2%;
  top: 2%;
}

.block3-info {
  position: absolute;
  right: 2%;
  top: 51%;
  bottom: 2%;
  width: 57%;
}

.block-main-header {
  font-size: 26px;
  color: var(--color-4);
  font-weight: 600;
}

.block-separator {
  color: var(--color-5);
}

.block-text {
  color: var(--color-5);
}

.block-lion-card {
  border: 5px solid var(--color-1);
  border-radius: 20px;
  background: none;
  margin: 2px;
  width: 98%;
  display: block;
  height: min-content;
  background: var(--color-3);
}

.card-lion-name {
  display: inline-block;
  width: 100%;
  background: var(--color-1);
  padding: 10px;
  margin: 0;
  font-size: larger;
  color: var(--color-6);
  border-radius: 10px 10px 0 0;
}

.card-link {
  display: flex;
  flex-wrap: wrap;
  align-content: flex-start;
  justify-content: space-around;
}

.card-link img {
  width: 100%;
  height: auto;
}

.lion-description {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-around;
  margin: 0;
  width: 100%;
  background: var(--color-1);
  align-content: stretch;
  padding: 10px;
  line-height: 1.3;
  color: var(--color-5);
}

/*?credits*/
.breadcrumb::after {
  content: "CSS от Polaris Penumbra (#419566)";
  position: absolute;
  right: 20%;
  z-index: 100;
  color: var(--color-5);
}
